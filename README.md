# ImageGeneration

- Use "make" in the terminal while at root of project
- A folder named build should be produced containting the output
- Run output by typing "./build/renderer > output.ppm" in the terminal
- A output.ppm file should be created allowing you to open and view the image

